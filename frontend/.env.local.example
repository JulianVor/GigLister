# Server-side base URL for calling the Spring Boot backend (Server Components, Server Actions).
API_BASE_URL=http://localhost:8080

# Same backend, reachable from the browser — used only for the live "Meintest du?"
# duplicate-suggestion lookup while typing a new band/location name.
NEXT_PUBLIC_API_URL=http://localhost:8080
