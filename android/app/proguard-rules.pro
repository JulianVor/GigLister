# Add project specific ProGuard rules here.
# Unused while isMinifyEnabled is false (see app/build.gradle.kts) - kept as a starting
# point for whenever a release build turns minification/shrinking on.
